/**
 * LogisticsTrack — Analyzer Settings
 * Impostazioni del video analyzer (YOLO model + parametri).
 * Contenuto estratto dalla vecchia Settings.jsx, ora integrata nella gerarchia /settings.
 */

// Re-esporta semplicemente il contenuto della vecchia Settings.jsx
// (identico, non dipende dalla location nella gerarchia)
export { default } from '../Settings';
